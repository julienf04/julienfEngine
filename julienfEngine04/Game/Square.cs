using System;
using julienfEngine1;

namespace julienfEngine1
{
    class Square : GameObject
    {
        public Square(Figure[] figures, int baseFigure = 0, bool visible = true, bool isUI = false, int layer = 0, int posX = 0, int posY = 0) :
                      base(figures, baseFigure, visible, isUI, layer, posX, posY)
        {
            
        }

        public void Start()
        {

        }

        public void Update()
        {

        }
    }
}